# Многострочные литералы в YAML
description:
  literal: |
    Этот текст
    сохраняет переводы строк
    как есть
  folded: >
    Этот текст
    сворачивается в одну строку
    с пробелами между словами
  
simple_literal: |
    Простой пример
    многострочного текста
    
simple_folded: >
    Простой пример
    складываемого текста
    на несколько строк

script: |
    #!/bin/bash
    echo "Hello World"
    exit 0
    
sql_query: |
    SELECT *
    FROM users
    WHERE active = true
    ORDER BY name;
